---
description: Implement non-destructive toggling between format modes, Markdown-to-HTML parsing, and premium toolbar toggle button styles.
---

# Workflow: Format Switching & Editor States

## Trigger
User types: `/rich_text`

## Steps

1. **Analyze**: Run `analyze_rich_text` to check format toggle states.
2. **Plan**: Run `plan_rich_text` to formulate implementation steps.
3. **Execute**: Run `execute_rich_text` to modify code in both `App.tsx` and `notepad.tsx`.
4. **Verify**: Run `pnpm run build` from the workspace root to check for build errors.
5. **Finalize**: Record session details in the changelog and update task queue statuses.

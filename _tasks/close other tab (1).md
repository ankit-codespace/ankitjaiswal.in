This is for both the app version and web version.

1.  Currently I see that when we close any normal tab which is not pinned, it asks for confirmation before deleting. This is a normal tab so we should not ask to delete it in both the web version and the app version. They should not ask. When we delete the pinned tab, it should ask for the confirmed version but it still shows that we can always restore it, right?
    
2.  Suppose I have clicked Close Other Tabs, it should not close other tabs including pinned tabs. It should always exclude the pinned tab from closing when we click Close Other Tabs using the right click, right? What do you think about it?
    
3.  Suppose if we close the tabs using Close Other Tabs but when I click using Ctrl+Alt+T or maybe Ctrl+Shift+T. Currently I am seeing in the web version it restores all the tabs. Is this the correct behavior or should we restore one by one? I think if we closed all tabs using Close Other Tab so all the tabs were closed at the same time, we can restore all of them at once. If they have closed one at a time then we would restore using one by one. I think this is the correct way. What do you think about it?
    

What else do you think we have broken?
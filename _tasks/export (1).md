1.  This is for the web version export button. In the app version when we click the export button it automatically selects any option that is suitable. When there is an image it selects the HTML file or maybe PDF. We should also do this same thing in the web version as well. Currently clicking export asks for it but it should not ask. Clicking the drop-down arrow near export should ask which file format. Fix this thing properly. What do you think?
    
2.  Do you see the option to toggle the focus mode or full screen mode in the web version? It is there in the web version because we can use it to enable the full screen mode that we used to do using the F11 key. In the app version we are already having the maximize/minimize button so we don't need this button in the app version. You remove that button from the app version. What do you think about it?
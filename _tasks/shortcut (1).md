We have many shortcuts in the web version as well as in the app version. In the web version we have some complications with some shortcuts that we can't use because they are already defined by Chrome itself. For example, control plus shift plus T is the classic shortcut for restoring the tabs in Chrome itself so if we use it in ours then it will be broken. We have used Alt instead of Shift. In our app version there is no such Chrome response, so we could use control shift T to restore the time. Suppose if a user is using both the app version and the web version then it won't be difficult for him or her to remember that in the web version we have to use Alt and in the app version we have to use Shift. What do you think? Should we make the app version use the Alt version as well? We know that typical shortcuts are control shift T, something like that, and we have that in the app version. For making it consistent because we can't use control shift T in the web version, the only thing we are left with is to make it consistent in the app as well, meaning using the same shortcuts in both places so it doesn't mess up and the user can do the same things on both versions. I am confused here about what to do, like making the app version behave like the web version or what. You need to think deeply, understand it, research about it deeply, and get to the conclusion. What do you think?

1.  Do the research.
    
2.  Create the implementation plans.
    
3.  Read the [instruction.md](http://instruction.md) file.
    
4.  Do not implement this here. Just read the [implementation.md](http://implementation.md) and understand what you have to do.
    
5.  You have to do the research and create the files. A new agent will handle the rest.
    

What do you think?


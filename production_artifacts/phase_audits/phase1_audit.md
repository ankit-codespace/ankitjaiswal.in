# Phase 1: Keyboard Shortcut Wiring

## Verification
- Added `Ctrl+F` and `Ctrl+Shift+F` event checks inside the `App.tsx` global key handler.
- Configured hook dependencies array with search states `showFind`, `showReplace`, and `closeFind`.
- Standardized the text highlighting trigger behavior.
